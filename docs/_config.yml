# The Site Configuration

# Import the theme
theme: jekyll-theme-chirpy

# The language of the webpage
lang: en

# Change to your timezone
timezone: Europe/Amsterdam

# jekyll-seo-tag settings
title: sevenrad-stills
tagline: Poetic interpretation of digital media
description: >-
  Algorithmic transformation for exploring digital media through
  the artistic voices of Rimbaud and Dominique de Groen.
  Extract, degrade, transform.

# Fill in the protocol & hostname for your site
url: "https://abossenbroek.github.io"

# GitHub repository
github:
  username: abossenbroek

# Social
social:
  # Change to your full name
  name: Anton Bossenbroek
  email: "" # Change to your email address
  links:
    # The first element serves as the copyright owner's link
    - https://github.com/abossenbroek/sevenrad-stills

# Site Verification (Optional)
google_site_verification: # Fill in to your verification string

# Web Analytics Settings (Optional)
analytics:
  google:
    id: # Fill in your Google Analytics ID

# Prefer color scheme setting
#
# Note: Keep empty will follow the system prefer color by default,
# and there will be a toggle to switch the theme between dark and light
# on the bottom left of the sidebar.
#
# Available options:
#
#     light  - Use the light color scheme
#     dark   - Use the dark color scheme
#
theme_mode: dark # Force dark mode for cyberpunk aesthetic

# The CDN endpoint for images
# Notice that once it is assigned, the CDN url
# will be added to all image (site avatar & posts' images) paths starting with '/'
#
# e.g. 'https://cdn.com'
img_cdn:

# the avatar on sidebar, support local or CORS resources
avatar:

# Boolean type, the global switch for TOC in posts
toc: true

comments:
  active: # The global switch for posts comments, e.g., 'disqus'. Keep it empty means disable
  # The active options are as follows:
  disqus:
    shortname: # Fill with the Disqus shortname
  # utterances settings
  utterances:
    repo: # <gh-username>/<repo>
    issue_term: # < url | pathname | title | ...>
  # Giscus options
  giscus:
    repo: # <gh-username>/<repo>
    repo_id:
    category:
    category_id:
    mapping: # optional, default to 'pathname'
    input_position: # optional, default to 'bottom'
    lang: # optional, default to the value of `site.lang`
    reactions_enabled: # optional, default to the value of `1`

# Self-hosted static assets, optional
assets:
  self_host:
    enabled: # boolean, keep empty means false
    # specify the Jekyll environment, empty means both
    # only works if `assets.self_host.enabled` is 'true'
    env: # [development|production]

pwa:
  enabled: false # the option for PWA feature (installable)
  cache:
    enabled: false # the option for PWA offline cache
    # Paths defined here will be excluded from the PWA cache
    # Usually its value is the `baseurl` of another website that
    # shares the same domain name as the current website
    deny_paths:
      # - "/example"

paginate: 10

# The base URL of your site
baseurl: ""

# Site Settings
kramdown:
  syntax_highlighter: rouge
  syntax_highlighter_opts: # Rouge Options › https://github.com/jneen/rouge#full-options
    css_class: highlight
    # default_lang: console
    span:
      line_numbers: false
    block:
      line_numbers: true
      start_line: 1

collections:
  tabs:
    output: true
    sort_by: order

defaults:
  - scope:
      path: "" # An empty string here means all files in the project
      type: posts
    values:
      layout: post
      comments: false # Enable comments in posts
      toc: true # Display TOC column in posts
      # DO NOT modify the following parameter unless you are confident enough
      # to update the code of all other post links in this project
      permalink: /posts/:title/
  - scope:
      path: _drafts
    values:
      comments: false
  - scope:
      path: ""
      type: tabs # see `site.collections`
    values:
      layout: page
      permalink: /:title/

sass:
  style: compressed

compress_html:
  clippings: all
  comments: all
  endings: all
  profile: false
  blanklines: false
  ignore:
    envs: [development]

exclude:
  - "*.gem"
  - "*.gemspec"
  - docs/_posts
  - tools
  - README.md
  - CHANGELOG.md
  - LICENSE
  - rollup.config.js
  - package*.json

jekyll-archives:
  enabled: [categories, tags]
  layouts:
    category: category
    tag: tag
  permalinks:
    tag: /tags/:name/
    category: /categories/:name/
